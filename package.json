{
  "name": "gavinos-pizzeria",
  "private": true,
  "version": "0.1.0",
  "description": "Gavino's Pizzeria — modern website redesign (identity preserved)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:pages": "cross-env VITE_BASE=/GavinosHeritage/ vite build",
    "preview": "vite preview",
    "preview:pages": "npm run build:pages && vite preview --base /GavinosHeritage/"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "vite": "^6.3.5"
  }
}
